{
  "name": "@s-dante/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "ai-documents": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "bun dist/index.js",
    "lint": "tsc --noEmit",
    "test": "echo \"No tests configured for cli yet\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
