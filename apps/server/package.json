{
  "name": "@ai-documents/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json --outDir dist",
    "start": "bun dist/index.js",
    "lint": "tsc --noEmit",
    "test": "echo \"No tests configured for server yet\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "elysia": "^1.4.29"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
